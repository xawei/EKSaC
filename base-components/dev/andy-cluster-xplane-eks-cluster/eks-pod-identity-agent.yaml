repoURL: https://csxawei.github.io/helm-repo
targetRevision: 1.2.0
namespace: kube-system
wave: "0"

helm:
  chart: eks-pod-identity-agent
  values:
    # Add any specific values for the EKS Pod Identity Agent here
    # For example:
    # replicaCount: 2
    # resources:
    #   limits:
    #     cpu: 100m
    #     memory: 128Mi